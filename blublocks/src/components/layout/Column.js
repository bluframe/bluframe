// @flow

import styled, { type StyledComponent } from "styled-components"

const Column: StyledComponent<any, mixed, HTMLDivElement> = styled.div`
  min-height: 1px;
  position: relative;
`

export default Column
